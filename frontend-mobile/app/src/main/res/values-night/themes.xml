<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.AccidentReportingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main Brand Color -->
        <item name="colorPrimary">@color/primary_container</item> <!-- Using Lavender as primary in dark mode -->
        <item name="colorOnPrimary">@color/primary</item> <!-- Deep Midnight text on Lavender -->

        <item name="colorPrimaryContainer">@color/primary</item>
        <item name="colorOnPrimaryContainer">@color/primary_container</item>

        <!-- Background Colors -->
        <item name="android:windowBackground">@color/dark_grey</item>
        <item name="android:colorBackground">@color/dark_grey</item>
        <item name="colorSurface">@color/surface_dark</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/primary_container</item>
        <item name="android:textColorSecondary">@color/tertiary</item>
        <item name="colorOnSurface">@color/primary_container</item>
    </style>
</resources>
